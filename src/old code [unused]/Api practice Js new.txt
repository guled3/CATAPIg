class React.Component {
    constructor(props) {
        super(props);
        this.state = {
            breed: "",
            GIF:: "",
            Category: "",
        }
    }
    render() {
        return (
            <div className = "bodydiv">
                <div className="titlediv"></div>
                <form onSubmit>={} </form>
                </div>
        )
    }
}

state = { 
    loading: true
}

export default class App extends React.Component { 
    .then(results => {
        return results.json();
    }.then(data => {
        let pictures =data results.map(pic) => {
            return{
                <div id={pic.picture.medium} />
                </div>
            }
        }
    })
    });

    var myMap = new Map();

    var keyString = "pic",
    keyObj = {}, 
    keyFunc = function () {};

  myMap.set(keyString, )  
export default class App extends React.Component {
    fetch("https://thecatapi.com")   
}
    this.setState{{pictures: pictures}};
    console.log("state", this.state.pictures);
})
}
    
render() {
    return <div className="Kittyteam">
        {this.state.loading ? <div>loading...</div>} 
}